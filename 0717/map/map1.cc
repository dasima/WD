#include <iostream>
#include <string>
#include <map>
using namespace std;
//这里Student没有比较规则，所以不能作为map的key
class Student
{
};
int main(int argc, const char *argv[])
{
//    map<Student, int> m;
   // Student s1;
   // m[s1] = 10; 

   // map<int, int> m1;
   // map<string, int> m2;
   // map<string, string> m3;
   // map<string, vector<string> > m4;//记得m4前>前有个空格
    return 0;
}
